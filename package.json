  {
  "name": "x-bookmarks-manager",
  "version": "1.0.0",
  "type": "module",
  "description": "Personal tool to scrape, manage, tag, and search X (Twitter) bookmarks",
  "scripts": {
    "dev": "npm --prefix app run dev",
    "build": "npm --prefix app run build",
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^26.0.0",
    "vitest": "^3.0.7"
  }
}
